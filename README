To compile, add the appropriate flags in the Makefile.cfg file.
Some predefined, but not fully tested, cases are available.

Run "make" to build.

Run "make install", binaries and test scripts will be placed in a directory
named "bin".

Run "make clean" to remove created files (optional).

You may need to run "make" more than once in some cases.


Contact: Ignacio Fdez. Galván (jellby@unex.es)
